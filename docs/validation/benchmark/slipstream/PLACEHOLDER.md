# Slipstream export goes here

No Slipstream benchmark export is checked in yet — this is a placeholder
so the folder exists in version control (git does not track empty
directories).

Generate one from a real study run:

```bash
python main.py export-study <study_name> --out docs/validation/benchmark/slipstream/<study_name>.csv
```

See [`../README.md`](../README.md).

Delete this file once a real export CSV is added here.
