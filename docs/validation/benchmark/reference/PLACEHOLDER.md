# Reference dataset goes here

No reference dataset is checked in yet — this is a placeholder so the
folder exists in version control (git does not track empty directories).

Drop a CSV here with at least the columns `AOA_deg, Velocity_m_s, CL, CD`,
sourced independently of Slipstream (published data, an established
benchmark case, or a separately-validated solver run). See
[`../README.md`](../README.md) and
[`../../VALIDATION.md`](../../VALIDATION.md) §9.

Delete this file once a real reference CSV is added here.
